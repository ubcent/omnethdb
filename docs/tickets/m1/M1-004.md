# Ticket

## Header

- Ticket ID: `M1-004`
- Title: Enforce Embedder Locking And Bootstrap Verification
- Milestone: `M1`
- Sprint: `SPRINT_PLAN_M1`
- Type: `verify`
- Status: `todo`
- Priority: `P0`

## Summary

Verify that bootstrap is real and that space identity cannot silently drift across incompatible embedding models.

## Goal

Prove that first-write bootstrap is persistent and that mismatched embedders are rejected cleanly.

## Scope

In scope:

- tests for initial space creation
- tests for embedder mismatch rejection
- tests for persistence of bootstrapped config

Out of scope:

- migration behavior
- retrieval weighting
- backup and restore

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [M1-003.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m1/M1-003.md)

## Deliverables

- verification suite for space bootstrap and embedder locking

## Acceptance Criteria

1. First-write bootstrap is proven against persisted state rather than only returned runtime values.
2. Writes with mismatched embedder model or dimension are rejected without corrupting space state.
3. Test coverage is sufficient to support later migration and retrieval work without redefining bootstrap semantics.

## Dependencies

- `M1-003`

## Traceability

Spec slices:

- Space Bootstrap And Configuration Locking
- Operational Config And Disk Layout

Capabilities:

- `CAP-01`
- `CAP-02`

UAT scenarios:

- `UAT-01`
- `UAT-02`
- `UAT-03`

Backlog items:

- `BL-006`

## Notes

This ticket should verify real persistence behavior, not test-only bootstrap shortcuts.
