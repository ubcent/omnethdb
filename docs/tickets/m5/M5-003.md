# Ticket

## Header

- Ticket ID: `M5-003`
- Title: Implement Relation Traversal And Audit Retrieval APIs
- Milestone: `M5`
- Sprint: `SPRINT_PLAN_M5`
- Type: `impl`
- Status: `done`
- Priority: `P0`

## Summary

Complete the cold-path inspection surface by exposing relation traversal and audit history retrieval.

## Goal

Implement `GetRelated` and `GetAuditLog` so callers can inspect explicit graph structure and lifecycle history without affecting hot-path retrieval.

## Scope

In scope:

- relation traversal by type and depth
- inclusion of historical and forgotten records in traversal
- audit retrieval by space and time range

Out of scope:

- retrieval expansion based on relations
- release evidence formatting
- migration workflow

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [M5-001.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m5/M5-001.md)
- [docs/tickets/m2/M2-002.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m2/M2-002.md)

## Deliverables

- `GetRelated` implementation
- `GetAuditLog` implementation

## Acceptance Criteria

1. Relation traversal returns linked memories according to explicit stored relations and requested depth.
2. Historical and forgotten records remain traversable for inspection use cases.
3. Audit retrieval exposes lifecycle history scoped to space and time without altering retrieval behavior.

## Dependencies

- `M5-001`

## Traceability

Spec slices:

- Inspector And Graph Traversal
- Auditability And Forget Records

Capabilities:

- `CAP-20`
- `CAP-21`

UAT scenarios:

- `UAT-23`
- `UAT-24`

Backlog items:

- `BL-037`

## Notes

Keep traversal semantics explicit and bounded; do not introduce inferred graph behavior here.

Implemented as explicit bounded traversal over stored relation adjacency plus chronological audit retrieval over append-only audit entries.
