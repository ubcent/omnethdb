# Ticket

## Header

- Ticket ID: `M5-006`
- Title: Verify Concurrent Write Rejection And Snapshot-Safe Reads
- Milestone: `M5`
- Sprint: `SPRINT_PLAN_M5`
- Type: `verify`
- Status: `done`
- Priority: `P0`

## Summary

Prove that concurrent writers cannot silently corrupt latest-version semantics and that reads never observe partial write state.

## Goal

Build the verification layer for optimistic-lock conflict behavior and read consistency during concurrent writes.

## Scope

In scope:

- one-winner contested update tests
- single-latest preservation checks
- read consistency tests during concurrent writes

Out of scope:

- migration-time read/write behavior
- performance benchmarking
- release-level evidence formatting

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [M5-005.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m5/M5-005.md)
- [docs/tickets/m4/M4-001.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m4/M4-001.md)

## Deliverables

- concurrency verification suite

## Outcome

- Added a contested-update test that proves two writers using the same `IfLatestID` produce one winner and one explicit `ErrConflict`.
- Added a concurrent read/write test that repeatedly calls `Recall` during a series of updates and verifies readers only ever observe one coherent live latest snapshot.

## Acceptance Criteria

1. Tests prove one contested writer succeeds and stale writers are rejected when optimistic locking is used.
2. Tests prove lineage state remains consistent with a single latest record after contention.
3. Tests prove reads observe coherent snapshots rather than half-applied write transitions.

## Dependencies

- `M5-005`

## Traceability

Spec slices:

- Concurrency And Consistency

Capabilities:

- `CAP-22`

UAT scenarios:

- `UAT-25`
- `UAT-26`

Backlog items:

- `BL-041`

## Notes

Use real persisted-state contention in tests where practical, not only mocked transaction behavior.
