# Ticket

## Header

- Ticket ID: `M4-006`
- Title: Verify Profile Layering And Candidate Search Separation
- Milestone: `M4`
- Sprint: `SPRINT_PLAN_M4`
- Type: `verify`
- Status: `todo`
- Priority: `P0`

## Summary

Prove that context assembly and candidate search remain distinct APIs with distinct semantics.

## Goal

Build the verification layer for `GetProfile`, orphan filtering, and `FindCandidates` behavior.

## Scope

In scope:

- static/episodic profile layer tests
- orphan filtering tests
- raw-cosine candidate search tests
- semantic divergence checks between `Recall` and `FindCandidates`

Out of scope:

- lineage inspection
- audit retrieval
- migration behavior

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [M4-003.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m4/M4-003.md)
- [M4-004.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m4/M4-004.md)

## Deliverables

- verification suite for profile layering and candidate search

## Acceptance Criteria

1. Tests prove `GetProfile` returns separate capped layers and includes derived memories in static results.
2. Tests prove orphaned derived memories can be excluded only when explicitly requested.
3. Tests prove `FindCandidates` remains raw-similarity search rather than retrieval-by-another-name.

## Dependencies

- `M4-003`
- `M4-004`

## Traceability

Spec slices:

- GetProfile Layered Retrieval
- FindCandidates Contract
- Orphaned Source Propagation

Capabilities:

- `CAP-11`
- `CAP-13`
- `CAP-14`

UAT scenarios:

- `UAT-13`
- `UAT-16`
- `UAT-17`

Backlog items:

- `BL-031`

## Notes

This ticket is the gate that keeps the three retrieval-facing APIs from collapsing into one fuzzy contract.
