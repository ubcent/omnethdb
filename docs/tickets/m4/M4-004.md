# Ticket

## Header

- Ticket ID: `M4-004`
- Title: Implement FindCandidates Raw Similarity Search
- Milestone: `M4`
- Sprint: `SPRINT_PLAN_M4`
- Type: `impl`
- Status: `todo`
- Priority: `P1`

## Summary

Add a relation-authoring search mode that finds semantically similar memories without applying retrieval trust and lifecycle ranking semantics.

## Goal

Implement `FindCandidates` as a raw-cosine search over live corpus by default, with explicit historical inclusion options.

## Scope

In scope:

- raw cosine candidate search
- default live-only behavior
- explicit inclusion of superseded and forgotten records

Out of scope:

- relation recommendation
- trust/confidence weighting
- profile assembly

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [docs/tickets/m1/M1-005.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m1/M1-005.md)

## Deliverables

- `FindCandidates` implementation

## Acceptance Criteria

1. `FindCandidates` uses raw cosine only.
2. Historical records are included only when explicitly requested.
3. The API remains advisory and does not imply relation choice or mutate stored state.

## Dependencies

- `M1-005`

## Traceability

Spec slices:

- FindCandidates Contract

Capabilities:

- `CAP-14`

UAT scenarios:

- `UAT-17`

Backlog items:

- `BL-030`

## Notes

This ticket should stay clearly separated from `Recall` to avoid semantic drift.
