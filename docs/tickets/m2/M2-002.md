# Ticket

## Header

- Ticket ID: `M2-002`
- Title: Implement Relation Validation For Updates And Extends
- Milestone: `M2`
- Sprint: `SPRINT_PLAN_M2`
- Type: `impl`
- Status: `todo`
- Priority: `P0`

## Summary

Enforce explicit relation rules so updates and contextual links remain structurally valid and auditable.

## Goal

Implement validation for `Updates` and `Extends` relations, including locality, target validity, and prohibited shapes.

## Scope

In scope:

- relation locality checks
- `Updates` validation
- `Extends` validation
- rejection of invalid or self-referential relation shapes

Out of scope:

- `Derives`
- graph traversal APIs
- retrieval-time relation use

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [M2-001.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m2/M2-001.md)

## Deliverables

- relation validation logic for `Updates` and `Extends`
- clear reject behavior for invalid relation authoring

## Acceptance Criteria

1. `Updates` and `Extends` are accepted only when they satisfy their documented structural rules.
2. Invalid relation targets, illegal self-relations, and cross-space relation attempts are rejected.
3. Relation validation composes cleanly with lineage state transitions rather than bypassing them.

## Dependencies

- `M2-001`

## Traceability

Spec slices:

- Relation Validation Semantics
- Lineage And Versioning Semantics

Capabilities:

- `CAP-04`
- `CAP-05`

UAT scenarios:

- `UAT-05`
- `UAT-06`

Backlog items:

- `BL-010`
- `BL-011`

## Notes

This ticket intentionally excludes derived relations to keep Milestone 2 bounded.
