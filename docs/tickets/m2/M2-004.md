# Ticket

## Header

- Ticket ID: `M2-004`
- Title: Implement Trust Resolution And Live Corpus Limit Enforcement
- Milestone: `M2`
- Sprint: `SPRINT_PLAN_M2`
- Type: `impl`
- Status: `todo`
- Priority: `P1`

## Summary

Complete the governance surface needed for later retrieval and storage control by implementing trust lookup and live corpus limit checks.

## Goal

Resolve trust from policy at runtime and enforce live memory limits so later retrieval and write behavior remain governed.

## Scope

In scope:

- trust resolution contract from `SpaceWritePolicy`
- default and override trust handling
- live corpus limit checks for stored memories

Out of scope:

- scoring engine
- profile limits during retrieval
- migration-time locking

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [M2-003.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m2/M2-003.md)

## Deliverables

- trust resolution helper surface
- live corpus limit enforcement logic

## Acceptance Criteria

1. Trust can be resolved from current policy defaults and per-actor overrides without persisting trust onto memory records.
2. Live corpus limit checks reject writes when configured caps are exceeded.
3. Governance logic is ready to be reused by later retrieval scoring without redefining trust behavior.

## Dependencies

- `M2-003`

## Traceability

Spec slices:

- Write Governance And Promotion

Capabilities:

- `CAP-17`
- `CAP-18`

UAT scenarios:

- `UAT-20`
- `UAT-21`

Backlog items:

- `BL-014`
- `BL-015`

## Notes

Even though retrieval scoring lands later, the trust contract should be fixed here.
