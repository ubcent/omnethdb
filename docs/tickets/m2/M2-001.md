# Ticket

## Header

- Ticket ID: `M2-001`
- Title: Specify And Implement Lineage Model And Latest-Switch Invariants
- Milestone: `M2`
- Sprint: `SPRINT_PLAN_M2`
- Type: `impl`
- Status: `todo`
- Priority: `P0`

## Summary

Turn the storage foundation into a versioned lineage model with one current truth per lineage.

## Goal

Implement the core lineage state model for roots and updates, including latest-version exclusivity and valid parent/root relationships.

## Scope

In scope:

- lineage state model for root and update cases
- latest-switch semantics
- parent/root/version field behavior
- rejection of invalid latest-state transitions

Out of scope:

- derived memory rules
- forget and revive
- concurrent conflict handling

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [SPRINT_PLAN_M2.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/SPRINT_PLAN_M2.md)
- [docs/tickets/m1/M1-005.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m1/M1-005.md)

## Deliverables

- lineage transition rules implemented in code
- stable latest-switch behavior for root and update flows
- foundational invariant checks for lineage state

## Acceptance Criteria

1. Root and update cases produce consistent `Version`, `ParentID`, `RootID`, and `IsLatest` state.
2. A lineage can have at most one latest memory after any valid write.
3. Invalid lineage transitions are rejected before corrupting persisted state.

## Dependencies

- `M1-001`
- `M1-005`

## Traceability

Spec slices:

- Lineage And Versioning Semantics

Capabilities:

- `CAP-04`

UAT scenarios:

- `UAT-05`

Backlog items:

- `BL-010`
- `BL-011`

## Notes

Keep the lineage model focused on root/update semantics; later lifecycle flows build on it.
