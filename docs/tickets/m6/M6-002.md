# Ticket

## Header

- Ticket ID: `M6-002`
- Title: Implement Full-Corpus Re-Embedding And Migration Completion
- Milestone: `M6`
- Sprint: `SPRINT_PLAN_M6`
- Type: `impl`
- Status: `done`
- Priority: `P0`

## Summary

Complete migration by re-embedding all records in a space and updating the locked embedder identity only after the corpus is consistent.

## Goal

Implement the operational body of migration, including live and historical records and completion-time config update.

## Scope

In scope:

- re-embedding live memories
- re-embedding historical memories
- updating persisted embedder identity and dimension after completion

Out of scope:

- crash-recovery verification
- backup/restore workflows
- retrieval benchmarking after migration

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [M6-001.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m6/M6-001.md)
- [docs/tickets/m1/M1-005.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m1/M1-005.md)

## Deliverables

- full-corpus migration implementation
- completion logic for new embedder config

## Outcome

- Added `MigrateEmbeddings` to re-embed all memories in a space, including historical versions.
- Completion updates persisted embedder identity and dimension only after the re-embedding transaction succeeds.
- Reads never see a mixed steady state because config promotion to the new embedder happens only at completion.

## Acceptance Criteria

1. Migration re-embeds both live and historical memories in the target space.
2. New embedder identity is persisted only after migration completes successfully.
3. The migrated space does not expose mixed old/new embedding state as a valid steady state.

## Dependencies

- `M6-001`

## Traceability

Spec slices:

- Embedding Migration Workflow

Capabilities:

- `CAP-23`

UAT scenarios:

- `UAT-27`

Backlog items:

- `BL-043`

## Notes

Historical embeddings are mandatory here, not an optional cleanup pass.
