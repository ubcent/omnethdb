# Ticket

## Header

- Ticket ID: `M3-004`
- Title: Implement Revive For Inactive Lineages
- Milestone: `M3`
- Sprint: `SPRINT_PLAN_M3`
- Type: `impl`
- Status: `done`
- Priority: `P0`

## Summary

Enable explicit reactivation of inactive lineages without mutating historical forgotten state.

## Goal

Implement `Revive` so it creates a new latest memory only for inactive lineages and preserves lineage kind rules.

## Scope

In scope:

- revive precondition checks
- new latest creation for inactive lineage
- parent linkage to forgotten latest
- kind preservation against root lineage

Out of scope:

- cross-kind evolution
- derived-specific revive behavior
- concurrency conflict semantics

## Inputs

- [ARCHITECTURE.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/ARCHITECTURE.md)
- [M3-003.md](/Users/dmitrybondarchuk/Projects/my/omnethdb/docs/tickets/m3/M3-003.md)

## Deliverables

- `Revive` implementation
- validation logic for active vs inactive lineage behavior

## Acceptance Criteria

1. Revive succeeds only when the lineage is inactive.
2. Revive creates a new latest memory linked to the forgotten predecessor without un-forgetting it.
3. Revive rejects kind changes that would bypass lineage governance.

## Dependencies

- `M3-003`

## Traceability

Spec slices:

- Revive Contract
- Forget And Inactive Lineages

Capabilities:

- `CAP-10`

UAT scenarios:

- `UAT-12`

Backlog items:

- `BL-024`

## Notes

Keep revive narrow and explicit; do not smuggle it into general update semantics.

Implemented as an explicit `Revive` lifecycle API. Current scope rejects revive for `KindDerived` lineages until derived-specific revive provenance is designed.
